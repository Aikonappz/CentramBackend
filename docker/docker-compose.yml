version: '3'
services:
#  redis:
#    image: "redis"
#    container_name: "centram_redis"
#    command: redis-server --requirepass sOmE_sEcUrE_pAsS
#    ports:
#      - "6379:6379"
#    volumes:
#      - $PWD/redis-data:/var/lib/redis
#      - $PWD/redis.conf:/usr/local/etc/redis/redis.conf
#    environment:
#      - REDIS_REPLICATION_MODE=master
#    networks:
#      - centram
  mysql:
    image: "mysql"
    #restart: always
    container_name: "centram_mysql"
    #command:
      #- "mysqld --innodb_buffer_pool_size=400M --innodb_buffer_pool_instances=1 --ft_min_word_len=1 --ft_stopword_file=''"
      #- ""
    environment:
      MYSQL_ROOT_PASSWORD: sam_coder
      MYSQL_DATABASE: centram
    ports:
      - "3307:3306"
    networks:
      - centram
networks:
  centram:
    driver: bridge