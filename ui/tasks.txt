Ramesh - tasks
--------------
Module label change to category -d 
submodule change to sub category -d
attachment shold below desc -d
Incident Details - > Incident Description -d
Save as draft require -d
subit and cancel -d
Update an Incident -> Update Incident -d
individual incident allocation -d
reopen required from user end -d


Notification -> My Notfications
Incident -> My group Incident
Incomming incident-> my assigned incident


My tasks
-------
1. after assignment notification mail - wip
2. after incident add/update notification mail -wip
3. count sla
4. assign round robin to user - yet to start


Raise incident -> createa an incident -d
SOLUTION_PROVIDED not required -d
after 15 days emp can't reopen the ticket -d
remove user email from incident listing -d



SLA ABOUT TO BREACH
Priority 1: SLA is 4 hours
If the SLA is nearing 2 hours, then an email notification should be triggered to the agent with a warning message.
If the SLA is nearing 3 hours, then another email notification should be triggered to the agent with a warning message.
If the SLA is breached after 4 hours, then the first level escalation mail should be triggered to the module lead (agent's manager).
If the ticket is not resolved after the 1st level escalation within 2 hours, then the second level escalation mail should be triggered to the delivery lead (module lead's manager).




support@centram.live/Welcome@12345#

https://letsencrypt.org/ -->ssl


1. EC2 -> provisioning is done
2. deploy and test application in aws
    2.1  s3 -> UI
    2.2. t2.micro -> serviec/redis/mysql
3. round robin
4. have to ask zoho if we can delete mail






REPORT
-----
1. WE CAN DIRECTLY CALL OUR DB.
2. INTERMIDIATOR/JOB



ssh -i "centram-key-stag.pem" ec2-user@ec2-65-1-100-215.ap-south-1.compute.amazonaws.com
scp -i "centram-key-stag.pem" "/home/sumit/Documents/ramesh/app/centram/ui/dist/centram-ui/ui.zip"  ec2-user@ec2-65-1-100-215.ap-south-1.compute.amazonaws.com:/tmp
scp -i "centram-key-stag.pem" /home/sumit/Documents/ramesh/app/centram/core-api/target/core-service-api.jar  ec2-user@ec2-65-1-100-215.ap-south-1.compute.amazonaws.com:/tmp




sudo service nginx start
sudo service nginx redis
sudo service nginx mysqld
sudo nohup java -Dspring.config.location=/home/ec2-user/application.properties -jar /home/ec2-user/core-service-api.jar >& /dev/null &

sudo nohup java -jar /home/ec2-user/core-service-api.jar -Dapp.allowed.origins=http://65.1.100.215,http://65.1.100.215:80 >& /dev/null &







